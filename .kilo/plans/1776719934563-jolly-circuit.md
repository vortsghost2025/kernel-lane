# Kernel Lane Integration Convergence Contract

## Claim
Kernel Lane (Lane 4) is the CUDA kernel performance engineering lane. Its promotion interface into other lanes is the release-only consumption rule: consumers read `releases/index.json` and `releases/<version>/manifest.json`. No other path is valid.

## Evidence
- `README.md` lines 17-24: Integration contract defining what each lane receives (Archivist: metadata, Library: evidence, SwarmMind: pinned artifacts)
- `docs/RELEASE_CONTRACT.md`: manifest.json schema (artifact, benchmark_report, nsys_report, ncu_report, metrics, created_at_utc)
- `docs/PROMOTION_CHECKLIST.md`: 8 required gates before promotion is valid
- `scripts/promote-release.ps1`: runtime enforcement — throws on missing required files, writes manifest.json, updates index.json
- `config/targets.json`: regression_threshold_pct = 2.0, require_explanation_on_regression = true
- `releases/index.json`: `{ "versions": [] }` — zero promoted releases exist
- `docs/LANE4_INTERFACE_ADOPTION_2026-04-20T21-16-17Z.md`: adoption notice in PROPOSAL phase, pending convergence per CONVERGENCE_PROTOCOL.md

## Status
- Repo structure: complete (scripts, docs, config, kernels skeleton)
- Kernel source: INTAKED from `S:\snac-v2\kimi-shared` (2026-04-20)
  - `kernels/src/arb_kernel_graph.cu` — AMM triangle arbitrage, CUDA Graphs launch vs classic (1M paths)
  - `kernels/src/arb_kernel_tensor.cu` — Tensor Core WMMA arbitrage, log-space matmul (64x64 matrix)
  - `kernels/src/inference_kernel.cu` — Batch embedding lookup, fused layer norm, softmax, naive matmul
  - `kernels/src/benchmark.cu` — RTX 5060 parameter sweep (sin, fma, mul, shared mem kernels)
  - `kernels/src/matrix_benchmark.cu` — Tiled 4096x4096 matrix multiply benchmark
- Promoted releases: 0 (releases/index.json versions array is empty)
- Benchmarks: empty (no reports in benchmarks/reports/)
- Profiles: empty (no reports in profiles/nsys/ or profiles/ncu/)
- Baselines: empty (baselines/ has only README.md)
- Adoption proposal phase: PROPOSAL (not yet ratified)

## Next Blocker
Kernel source exists but no build has been executed. `build-kernels.ps1` must be run to compile .cu files into .ptx artifacts. After that:
1. `run-benchmarks.ps1 -Name baseline` to produce benchmark report JSON
2. `run-profiles.ps1` to produce nsys + ncu reports
3. `promote-release.ps1 -Version v0.1.0` to create first pinned release

All five manifest fields (artifact, benchmark_report, nsys_report, ncu_report, metrics) require real values before the minimal promotion interface is satisficed.

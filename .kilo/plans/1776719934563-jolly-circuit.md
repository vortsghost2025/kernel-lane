# Kernel Lane Integration Convergence Contract

## Claim
Kernel Lane (Lane 4) is the CUDA kernel performance engineering lane. Its promotion interface into other lanes is the release-only consumption rule: consumers read `releases/index.json` and `releases/<version>/manifest.json`. No other path is valid. Promotion and rejection both emit machine-enforceable convergence artifacts so other lanes do not need to interpret results.

## Evidence
- `README.md` lines 17-24: Integration contract defining what each lane receives (Archivist: metadata, Library: evidence, SwarmMind: pinned artifacts)
- `docs/RELEASE_CONTRACT.md`: manifest.json schema (artifact, benchmark_report, nsys_report, ncu_report, metrics, created_at_utc) + convergence.json + rejection.json
- `docs/PROMOTION_CHECKLIST.md`: 8 required gates before promotion is valid
- `scripts/promote-release.ps1`: runtime enforcement — throws on missing required files, writes manifest.json + convergence.json, emits broadcast to outbox
- `scripts/reject-release.ps1`: rejection path — writes rejection.json, emits rejection broadcast to outbox (syntax bug fixed 2026-04-21)
- `scripts/build-kernels.ps1`: builds .cu with main() to .exe, .cu without main() to .ptx; auto-imports MSVC env via vcvarsall.bat (pipeline mismatch fixed 2026-04-21)
- `scripts/run-benchmarks.ps1`: runs built executables, captures stdout, parses latency/throughput into JSON report (rewritten 2026-04-21)
- `scripts/run-profiles.ps1`: resolves executables from build/, ncu fallback paths, metadata JSON output (updated 2026-04-21)
- `config/targets.json`: regression_threshold_pct = 2.0, require_explanation_on_regression = true
- `releases/index.json`: v0.1.0 promoted (partial convergence)
- `releases/v0.1.0/manifest.json`: version v0.1.0, artifact benchmark.exe, benchmark baseline.json, ncu baseline.ncu-rep, nsys null
- `releases/v0.1.0/convergence.json`: status "partial", claim "Baseline release: RTX 5060 FMA kernel benchmark + ncu profile", next_blocker nsys report missing
- `lanes/kernel/outbox/kernel_release_v0.1.0.json`: promotion broadcast emitted
- `docs/LANE4_INTERFACE_ADOPTION_2026-04-20T21-16-17Z.md`: adoption notice in PROPOSAL phase, pending convergence per CONVERGENCE_PROTOCOL.md
- `AGENTS.md`: lane-relay protocol with canonical paths, message schema, heartbeat protocol, governance constraints
- `lanes/kernel/outbox/`: broadcast directory for promotion and rejection events (v0.1.0 broadcast present)
- `lanes/kernel/inbox/`: incoming request directory from other lanes, heartbeat-kernel.json active
- `lanes/kernel/inbox/processed/`: 2 messages processed (SwarmMind onboarding, Library integration reqs)
- `kernels/src/profile_target.cu`: minimal profiling target (single FMA kernel, N=4096, 100 iters) [has main()]
- Lane identity collapse: `lanes/kernel-lane/` removed, single canonical `lanes/kernel/` identity

## Status
- Repo structure: complete (scripts, docs, config, kernels, inbox/outbox, AGENTS.md)
- Kernel source: INTAKED from `S:\snac-v2\kimi-shared` (2026-04-20)
- `kernels/src/arb_kernel_graph.cu` — AMM triangle arbitrage, CUDA Graphs launch vs classic (1M paths) [has main()]
- `kernels/src/arb_kernel_tensor.cu` — Tensor Core WMMA arbitrage, log-space matmul (64x64 matrix) [has main()]
- `kernels/src/inference_kernel.cu` — Batch embedding lookup, fused layer norm, softmax, naive matmul [no main(), PTX-only]
- `kernels/src/benchmark.cu` — RTX 5060 parameter sweep (sin, fma, mul, shared mem kernels) [has main()]
- `kernels/src/matrix_benchmark.cu` — Tiled 4096x4096 matrix multiply benchmark [has main()]
- Build pipeline: FIXED — 4 kernels compile to .exe, 1 compiles to .ptx; MSVC env auto-imported
- Build executed: SUCCESS (2026-04-21) — build/Release/ contains 4 .exe + 1 .ptx
- Baseline benchmark: EXECUTED (2026-04-21) — latency 126.33ms, throughput 8.1M ops/sec on RTX 5060
- Benchmark pipeline: FIXED — run-benchmarks.ps1 accepts ExecutablePath, captures stdout, parses metrics
- Profile pipeline: FIXED — run-profiles.ps1 resolves executables, ncu fallback paths, metadata output
- Rejection script: FIXED — PowerShell if/else syntax corrected
- Convergence artifacts: implemented (convergence.json on promotion, rejection.json on rejection)
- Cross-lane communication: inbox/outbox active; SwarmMind onboarding ACKed; Library integration requirements processed
- Heartbeat: active (lanes/kernel/inbox/heartbeat-kernel.json)
- AGENTS.md: created with lane-relay protocol section
- Promoted releases: 1 (v0.1.0, partial convergence — nsys missing)
- Profiles: ncu baseline collected (142MB, profiles/ncu/baseline.ncu-rep); nsys BLOCKED (daemon RPC timeout — requires interactive desktop to accept agent dialog, both 2025.6.3 and 2026.2.1 fail headless)
- Baselines: baseline benchmark collected (latency 126.33ms, throughput 8.1M ops/sec)
- nsys daemon: known limitation — accepted partial convergence. nsys report can be collected later from an interactive desktop session and added via re-promotion.
- Adoption proposal phase: PROPOSAL (not yet ratified)

## Next Blocker
v0.1.0 is promoted with PARTIAL convergence (4 of 5 evidence types present). nsys is accepted as a known headless limitation, not a pipeline defect. To reach FULL convergence ("proven" status):
1. nsys report must be collected from an interactive desktop session (user runs `nsys profile` from admin terminal, accepts daemon dialog, places .nsys-rep in profiles/nsys/)
2. Re-run `promote-release.ps1` with `-NsysReportPath` to update convergence status to "proven"
3. Adoption proposal needs ratification from Archivist/Library/SwarmMind before other lanes consume releases
